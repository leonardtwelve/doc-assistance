{
  "name": "doc-assistance",
  "version": "0.1.0",
  "private": true,
  "description": "Parcours d'assistance en assurance : déclaration de sinistre côté client, vérification IA des pièces justificatives, aide au gestionnaire.",
  "scripts": {
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{json,md,yml,yaml,css,html}": [
      "prettier --write"
    ]
  },
  "devDependencies": {
    "eslint": "^10.2.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "prettier": "^3.8.3"
  }
}
